# Ignore log collection output
logs-*/
*.log

# Ignore local data directories (docker volumes handle persistence)
data/
*.db
*.idb

# Ignore any local test scripts or notes
# Note: Official test scripts in test-scripts/ directory are NOT ignored
test-*.sh
!test-scripts/test-*.sh
notes.txt
TODO.txt

# Temporary files
*.tmp
*.temp
tmp/

# Backup files
*.bak
*.backup

# OS-specific files
.DS_Store
Thumbs.db

# Local testing configurations
*-local.yml
*-local.conf

# Static nodes configuration 
# IMPORTANT: Template files (with PLACEHOLDER values) are committed to git
# These provide the initial configuration structure for docker-compose mounts
# 
# After first run, the fukuii-cli sync-static-nodes command updates these files with actual enode IDs
# The updated files with real enode IDs are ignored to prevent committing runtime-generated values
conf/node*/static-nodes.json

# Configuration files - base configs are committed, but node-specific runtime configs are ignored
# This prevents runtime-modified configs from being committed accidentally
conf/node*/gorgoroth.conf.bak
conf/node*/*.conf.local

# Log files - both runtime logs and log collection outputs are ignored
logs/
*.log
conf/node*/logback*.xml.local
