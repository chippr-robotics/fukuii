# Heap increased to 8GB: Ethash DAG is ~3.5GB in JVM heap for current Mordor epoch
# Heap 3GB: 2GB OOM'd during trie finalization (2.7M accounts). No DAG needed (mining off).
-J-Xmx5g -J-Xms2g -J-Xss10M -J-XX:+UseG1GC -J-XX:MaxGCPauseMillis=200
