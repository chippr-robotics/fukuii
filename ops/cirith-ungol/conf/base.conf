# Cirith-Ungol base configuration proxy
# Mirrors the built-in Fukuii base configuration bundled inside the distribution
# so that our environment-specific overrides (etc.conf) can continue to "include base.conf"
# while mounting only files from this directory into /app/conf.

include classpath("conf/base.conf")
