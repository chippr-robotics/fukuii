name: Hive · rpc

on:
  push:
    branches: [main, master, develop]
    paths:
      - 'src/**'
      - 'build.sbt'
      - 'version.sbt'
      - 'project/**'
      - 'hive/**'
      - '.github/workflows/_hive-sim.yml'
      - '.github/workflows/hive-rpc.yml'
  schedule:
    - cron: '10 4 * * *'
  workflow_dispatch:
    inputs:
      hive_ref:
        description: 'ethereum/hive git ref'
        required: false
        default: 'master'
      sim_timelimit:
        description: 'Per-sim timelimit (e.g. 40m)'
        required: false
        default: '40m'

permissions:
  contents: read

concurrency:
  group: hive-rpc-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'push' }}

jobs:
  run:
    uses: ./.github/workflows/_hive-sim.yml
    with:
      sim: ethereum/rpc
      label: rpc
      sim_timelimit: ${{ github.event.inputs.sim_timelimit || '40m' }}
      hive_ref: ${{ github.event.inputs.hive_ref || 'master' }}
